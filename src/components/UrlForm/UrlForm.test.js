import React from 'react';
import { shallow } from 'enzyme';
import URLForm from './URLForm';

describe('URL Form', () => {
  it('renders the inputs in a form', () => {
    // const wrapper = shallow(<URLForm />);
    // const title = <form> </form>;
    //
    // expect(wrapper.contains(title)).toEqual(true);
  });
});
